---
description: Agora 项目核心约定：技术栈、架构、命名、文档规范
alwaysApply: true
---

# Agora 项目约定

## 技术栈

- **后端**: Go 1.23 + chi router + SQLite (modernc.org/sqlite)
- **前端**: React 18 + TypeScript + Vite + Tailwind CSS (dark mode)
- **GitHub SDK**: google/go-github v68
- **部署**: Docker Compose (Nginx + Go 二进制 + 前端 Nginx)

## 架构规则

### 后端目录
```
backend/
├── cmd/server/main.go        # 入口，路由注册
├── internal/
│   ├── config/               # 环境变量配置
│   ├── database/             # SQLite 初始化 + 迁移
│   ├── handler/              # HTTP handlers (一个文件一个资源)
│   ├── middleware/            # 中间件 (auth)
│   ├── model/                # 数据模型
│   ├── protocol/             # .agora.yaml 协议解析
│   └── service/              # 外部服务封装 (GitHub API)
```

### 前端目录
```
frontend/src/
├── layouts/    # 布局组件
├── pages/      # 页面组件 (按路由)
├── components/ # 可复用组件
└── lib/        # API 客户端、工具函数
```

## 命名与代码规范

### Go
- Handler 文件按资源拆分: `project.go`, `document.go`, `skill.go`, `release.go`
- 所有错误统一通过 `respondError(w, status, msg)` 返回
- 成功响应统一通过 `respondJSON(w, status, data)` 返回
- 路由统一挂在 `/api` 前缀下
- 每个 handler 文件不超过 200 行
- model 和 handler 不耦合，service 层做外部调用

### 前端
- 组件类名使用 Tailwind `@layer components` 的 agora-* 前缀类，不裸写 hex 颜色
- 动画统一用 `animate-fade-in` / `animate-slide-up`
- API 调用统一通过 `src/lib/api.ts`，自动带 Token
- 页面组件放 `pages/`，一个路由一个文件

## 数据存储

- Agora 不存储文档文件，只做元数据索引
- 文档内容由各项目自身仓库管理，Agora 通过 DB 记录文档路径、类型、标签等元信息
- 数据库: SQLite，路径 `${AGORA_DB_DIR}/agora.db`
- 各项目通过 `.agora.yaml` 声明其文档列表，Agora 扫描后写入 DB 索引
- Agora 自身文档存在项目根目录的 `docs/` 下（不属于 Agora 运行时数据）

## 鉴权

- 通过 `AGORA_AUTH_TOKEN` 环境变量控制，空值跳过鉴权
- 前端将 Token 存 `localStorage.agora_token`，自动附加 Authorization header
- 将来升级到 GitHub OAuth 时，只改 middleware 和登录页

## 文档化要求

- 每次开发会话都需要记录：问题讨论、设计决策、bug 修复过程
- Agora 自身文档存在项目根目录的 `docs/` 下，按类型分目录: design / dev_note / changelog / summary
