---
description: 开发过程中自动记录对话、设计决策和调试过程的规范
alwaysApply: true
---

# 开发文档自动记录

每次开发会话结束后，必须在 Agora 中记录以下内容（本项目作为 Agora 管理的第一个项目）：

## 记录内容

| 类型 | 何时记录 | 存储位置 |
|------|---------|---------|
| **设计讨论** | 每次讨论架构/技术决策 | `dev_note` — 标题格式 `设计讨论-{主题}-{日期}` |
| **Bug 修复** | 每次修复 bug 后 | `changelog` — 标题格式 `修复-{问题简述}-{日期}` |
| **代码审查** | Bugbot/Security review 后 | `changelog` — 标题格式 `代码审查-{日期}` |
| **阶段总结** | 每个 Tier 完成后 | `summary` — 标题格式 `阶段总结-Tier{N}-{日期}` |

## 记录格式

每篇文档必须包含：
- **背景**: 为什么做这个
- **讨论过程**: 关键问答摘要
- **决策**: 最终决定及理由
- **代码变更**: 涉及的文件列表

```
# {标题}

## 背景
...

## 讨论过程
- Q: ...
- A: ...

## 决策
...

## 变更文件
- backend/internal/handler/project.go：修复了 xxx
```

## 执行时机

- 每次对话结束前，检查是否有需要记录的内容
- 审查类的发现需及时记录（review findings）
- 不需要为每次小改动都记录，判断标准：涉及设计讨论、架构变更、bug 修复的都需要
